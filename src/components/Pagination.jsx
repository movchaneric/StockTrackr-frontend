import styled from "styled-components";

const Pagination = () => {
  return <StyledPagination>Implement pagination</StyledPagination>;
};

const StyledPagination = styled.section`
  background-color: green;
  display: flex;
  justify-content: end;
`;

export default Pagination;
